{
  "name": "turismo-itagui",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo del portal Turismo Itagüí: API REST de Express (backend/) y SPA de React + Vite (frontend/). En producción un único servicio compila el frontend y el backend lo sirve junto a la API.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm ci --prefix backend --omit=dev && npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/src/index.js",
    "seed": "npm run seed --prefix backend",
    "init-db": "npm run init-db --prefix backend"
  }
}
